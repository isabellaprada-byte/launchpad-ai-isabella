apiVersion: v1
kind: Service
metadata:
  name: census-frontend
  namespace: census-portal
spec:
  selector:
    app: census-frontend
  ports:
    - port: 3000
      targetPort: 3000
  type: ClusterIP
